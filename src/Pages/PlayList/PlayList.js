import React from "react";
import styled from "styled-components";

const PlayList = ({ match }) => {
  return <PlayListWrap></PlayListWrap>;
};

export default PlayList;

const PlayListWrap = styled.div`
  width: 100vw;
  height: 400px;
  background-color: #1d1d1d;
`;
